// Глобальные обьекты, с помощью них можно получить доступ к инфорвации или свойствам браузера, например размеру окна браузера
// Также есть глобальный объект в nodeJS - global
// Так же есть универсальный объект для обоих сред - globalThis (это тот же оъект просто работает и там и там)
console.log(window.innerHeight);

// так же есть console 

